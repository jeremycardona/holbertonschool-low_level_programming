/**
 * _memset - name
 * Description: something
 * Return: value.
 * @s: string
 * @b: char
 * @n: uns int
 */
char *_memset(char *s, char b, unsigned int n)
{
	(*s);
	(b);
	('0' + n);
	return (s);
}
