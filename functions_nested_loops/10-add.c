#include "main.h"
/**
 * add - add two integers and returns the result
 * Return: the sum of two integers
 * Description: function that adds two integers and returns this sum.
 */
int add(int a, int b)
{
	return (a + b);
}
