/**
 * _memset - something ill try later
 * Description: something ill describe later.
 * Return: something.
 * @s: ba
 * @b: chjar
 * @n: nn
 */
char *_memset(char *s, char b, unsigned int n)
{
	return (s);
}
