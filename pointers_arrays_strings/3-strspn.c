/**
 * _strspn - something ill try later
 * Description: something ill describe later.
 * Return: something.
 * @s: ba
 * @accept: soem
 */
unsigned int _strspn(char *s, char *accept)
{
	return (s);
}
